import React from 'react';
import CardTitle from "./CardTitle";
import 

function CardHeader({cardTitle, sideElement}) {
    return (
        <div>
            
        </div>
    )
}

export default CardHeader
